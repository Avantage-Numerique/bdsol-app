/**
 * Temp ? object to avoid duplicating coords and zoom while implementing the map components.
 * @type {{rouyn: {coords: number[], zoom: number}}}
 */
const cities = {
    "rouyn": {
        coords: [48.236,-79.015],
        zoom: 14
    }
};

export default cities;