import { render, screen } from '@testing-library/react'
import Spinner from './Spinner'

/*
test("Test le fonctionnement du Spinner", () => {
    render(<Spinner />)
    const spinner = screen.getByTestId('spinner')
    expect(spinner).toHaveStyle({position: 'absolute'})
})
*/